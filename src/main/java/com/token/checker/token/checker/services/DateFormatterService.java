package com.token.checker.token.checker.services;

public interface DateFormatterService {
    /**
     * Gets the now formmated timestamp in ISO 8601 format
     * @return the timestamp in the format
     */
    String nowFormmattedTimestamp();
}
